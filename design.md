# 水浒维基 · 设计规范 (design.md)

> 版本 v1.5 · 2026-09-20（v1.5：新增 §5.17 事件因果链；v1.4：§5.16 地名；v1.3：§5.15 结局；v1.2：§5.14 其他人物；v1.1：§5.12 名场面、§5.13 词条联动）
> 本文件是全站视觉的唯一标准。改设计 = 改这里 + 改 `scripts/assets/style.css`，两者必须同步。

---

## 1. 主视觉（Design Concept）

**「宋版书 × 数字刻本」**——以宋代雕版印刷的版式美学为底，用现代排版工程重构。

三个关键词：

| 关键词 | 含义 | 落地表现 |
|---|---|---|
| **刻本感** | 宋版书的框线、留白、对仗题 | 信息框用双边框（外粗内细，仿版框）；回目对仗独立成行；印章点缀 |
| **骨架清** | 结构分明、无多余装饰 | 细墨线分隔、克制圆角（≤4px）、不用阴影堆层次 |
| **缀朱砂** | 唯一强调色用在刀刃上 | 朱砂只给：实体高亮、印章、强调按钮、交互 hover |

**禁止**：渐变大背景、毛玻璃、卡片阴影、圆角 >4px、emoji 图标、超过两种强调色同时出现在一个屏区。

---

## 2. 色彩系统

### 2.1 色板（CSS 变量即 token）

| Token | 色值 | 用途 | 备注 |
|---|---|---|---|
| `--paper` | `#F6F1E7` | 页面底色（宣纸） | 全站唯一页面底色 |
| `--paper-hi` | `#FBF8F0` | 卡片、输入框、表头底 | 比纸面亮半度 |
| `--ink` | `#2A2723` | 正文墨（松烟墨） | 对比度 12.6:1 |
| `--ink-2` | `#4E4638` | 次级文字、诗词 | |
| `--ink-3` | `#8B8171` | 辅助文字、段号、meta | |
| `--cinnabar` | `#C03A2B` | **朱砂：实体高亮、印章、强调** | 对比度 5.5:1 ✓ |
| `--cinnabar-deep` | `#9E2B1F` | 朱砂 hover 态 | |
| `--dai` | `#3A5A6E` | 黛蓝：地名实体、纯导航链接 | 对比度 7:1 ✓ |
| `--gold` | `#A8842C` | 金：座次数字、徽章 | 仅小面积点缀 |
| `--line` | `#E3DAC6` | 细分隔线 | 1px 专用 |
| `--ink-bg` | `#211D18` | 墨底（topbar / footer） | 其上文字用 `#CFC7B4`/`#F0EADB` |

### 2.2 用色规则

- **朱砂用量 ≤ 屏区 10%**：它是印章，不是墙漆。
- **实体分两级，两色不混用**：一级实体（人名/绰号）= 朱砂；二级实体（地名）= 黛蓝。理由：正文里地名与人物同处一句（「林冲雪夜上梁山泊」），同色会让读者无法在扫读时分辨「这是人还是地」；而地名别名（梁山/水泊/东京）在全书命中逾千次，若一并走朱砂，朱砂用量必然突破 10% 红线，反而废掉「唯一强调色」。
- **朱砂唯一性不变**：朱砂只给人物实体、印章、强调按钮、交互 hover；地名**永不**取用朱砂，包括其 hover 态（hover 底用黛蓝 10%）。
- 普通跳转链接（导航、chips、「去原文」）= 黛蓝；与地名同色，靠**点状下划线 + 悬浮卡**区分（地名必有 `.ent-tip`，普通链接没有）。
- 金色只给「座次/排名」语义，不作装饰。
- 墨底区块（topbar、footer）下缘 2px 朱砂线收边。

---

## 3. 字体系统

### 3.1 字体栈

```css
--font-song:  "Songti SC","STSong","Noto Serif CJK SC","Source Han Serif SC","SimSun",serif;
--font-title: "STZhongsong","华文中宋","Songti SC","Noto Serif CJK SC",serif;
```

- 正文一律 `--font-song`；标题/印章/回目一律 `--font-title` + `font-weight:700`。
- 不用黑体；数字与英文随中文字体回退。

### 3.2 字阶（比例 ≈ 1.25）

| Token | 字号 | 行高 | 用途 |
|---|---|---|---|
| `--t-xs` | 12px | 1.5 | 段落编号、卡片 meta |
| `--t-sm` | 14px | 1.6 | 辅助说明、导航 |
| `--t-ui` | 16px | 1.6 | 界面正文、chips |
| `--t-body` | 19px | 2.05 | **正文阅读**（首行缩进 2em） |
| `--t-lg` | 21px | 1.7 | 诗词、引文 |
| `--t-h2` | 26px | 1.4 | 区块标题 |
| `--t-h1` | 34px | 1.35 | 回目对仗题、词条名 |
| `--t-hero` | 56px | 1.2 | 首页主标题（字距 12px） |

移动端（≤640px）：`--t-hero`→36px，`--t-body`→18px，栏宽 100%。

---

## 4. 版式与间距

- **阅读栏宽** `max-width: 720px`（约 36 字/行）；宽栏页面（图鉴/搜索）`max-width: 960px`。
- **间距 token**：`--s1:4px --s2:8px --s3:12px --s4:16px --s5:24px --s6:40px --s7:64px`，组件内外距只能取这些值。
- 段落间距 `--s4`；区块间距 `--s7`；标题上 `--s6` 下 `--s4`。
- 分隔一律 1px `--line` 细线，不用色块分割。

---

## 5. 组件规范

### 5.1 Topbar（墨底）
墨底 `#211D18`，下缘 2px 朱砂。左侧：**朱砂方印「水浒」**（白字两行，宋体粗）+ 站名（白，字距 3px）；右侧导航 `#CFC7B4`，hover 变白，下划 1px 朱砂线。

### 5.2 印章（.seal）
朱砂方底 + 白字 + 内描边（仿印泥边框），圆角 3px。尺寸两档：topbar 26px、hero 64px。**全站每屏至多一方印。**

### 5.3 首页 Hero
米白底 + 底部极淡水波纹（SVG，透明度 ≤30%）；居中大印 → 主标题 56px/字距 12px → 一句 tagline（ink-2）→ 统计行（ink-3）→ 按钮组。不加第二张图、不加动画。

### 5.4 章节页
- **题区**：回数小字（字距 6px，ink-3）→ 对仗回目两行（34px/700，居中）→ 朱砂小菱形「◆」收束。
- 正文 19px/2.05，首行缩进 2em；诗词不缩进、左移 4em、墨色降一档（ink-2）。
- **段落编号（Purple Numbers）**：段末 12px 朱色 `[n]`，透明度 0.35，hover 变实；绝对定位放在段尾行内，不影响缩进。
- 上下回导航：题区上方与正文末尾各一条，14px ink-3，1px 细线隔开。

### 5.5 实体高亮（.ent）+ 悬浮卡（.ent-tip）
- 人名/绰号：朱砂 + 1px 点状下划线（50% 透明）。
- **悬浮卡**：hover 实体时上方浮出墨底白字小卡（绰号 · 星号 · 座次），纯 CSS `:hover` 实现，180ms 淡入；卡在行首/行尾允许被裁切，不强制避让。
- 词条跳转：点击进 `wiki/<姓名>.html`。

### 5.6 图鉴卡片（.card）
`--paper-hi` 底 + 1px `--line` 边；hover 边变朱砂、上浮 2px。卡内结构：座次（金，小字）→ 星号（ink-2）→ 姓名（21px 粗）→ 绰号（朱砂）→ **结局标签**（右对齐起头，`align-self:flex-start`）→ 出场回数（ink-3）。栅格 `minmax(150px,1fr)` 自适应。

### 5.7 信息框（.infobox，刻本框）
**双边框**：外 2px 墨 + 内 1px 墨（`outline` 偏移 3px 实现，仿宋版版框）。表头格 `--paper-hi` 底；行内 1px `--line`。只用于词条页核心信息卡。

### 5.8 按钮
- 线框按钮（默认）：1px 朱砂边 + 朱砂字，hover 填朱砂白字。
- 墨底按钮（主行动）：填 `--ink`，白字，hover 变 `--cinnabar`。
- 圆角一律 3px；无阴影。

### 5.9 Chips（回目标签）
13–14px，1px `--line` 方角（radius 2px），hover 边变朱砂。禁用胶囊（radius 999 太现代）。

### 5.10 搜索
输入框无边框、只留 2px 墨下划线，聚焦变朱砂线；命中高亮 `mark` 用 `#F2D16B`（鹅黄，不用荧光黄）；结果定位行 = 金色回目 + 段落号。

### 5.11 Footer
墨底 + 上缘 2px 朱砂，13px `#A79C88`，三行以内：底本声明 / 许可 / 站内链接。

### 5.12 名场面（事件库，v1.1 新增）

- **幕分组（.act-head）**：回数范围小字（ink-3）+ 幕名（h2），下 1px 细线；四幕 = 好汉出场 / 梁山聚义 / 招安风云 / 南北征战。
- **事件卡（.event-card）**：同图鉴卡规范（paper-hi 底 + 1px line 边 + hover 朱砂边/上浮 2px），栅格 `minmax(280px,1fr)`。卡内顺序：类型小标签（金色小字，**类型不赋专属色**——守朱砂唯一原则）→ 名称（21px 粗宋）→ 概述（ink-2 小字）→ meta 行（回目 · 地点 · 人物，ink-3）。
- **原文摘录（.excerpt）**：左 3px 朱砂边条 + 左缩进 s4 + ink-2；摘录用「」包裹；下挂「去原文」黛蓝链接（回目+段号锚点）。
- **事件详情页**：题区（幕名+类型小字 / 事件名 h1 / ◆）→ 刻本框信息表（含「线索」行，v1.5）→ 概述 → 原文摘录 → **因果区（前因/后果，v1.5，见 §5.17）** → 出场人物 chips → 上一幕/下一幕导航（复用 .crumbs）。

### 5.13 词条页联动（v1.1 新增）

人物词条页在「出场回目」之后加「相关名场面」区，chips 链接事件页；与事件页「出场人物」互为反向链接。

### 5.14 其他人物（v1.2 新增）

- 非108将人物与108将**同套视觉**：同款卡片、同款刻本框词条页、同款朱砂高亮；区别仅在卡片/词条字段（无星号座次，显示分类+一句话简介）。
- 悬浮卡内容：108将 = 绰号·星号·座次；其他人物 = 分类·简介。
- 图鉴页在天罡/地煞之后按分类分区（梁山前史/朝廷官府/市井江湖/庄堡豪强/田虎·王庆/方腊/辽国/神异），不为分类赋专属色。

### 5.15 结局（v1.3 新增）

108 将结局是**有终局、有统计**的数据，视觉上要把「殁于王事」与「得以善终」的对比读出来，但**不靠颜色**——守 §2.2 朱砂唯一原则。

- **结局标签（.end-tag）**：同 chips 规范（1px `--line` 方角、`--paper` 底、12px ink-2）；`end-none` 未录用虚线边 + ink-3。**八个类型共用一种样式，不作色阶**。
- **图鉴卡**：绰号下方加结局标签（见 §5.6），`title` 属性挂结局原文，hover 可见。
- **图鉴页统计行**：`.note.end-stats`，一排 chips，每个链到结局页对应分组锚点（`endings.html#catN`），末尾「查看全部 108 条结局 →」。
- **词条页**：刻本框末行加「结局」行（标签 + 地点 + 出处回目）；「出场回目」之前另起 `<h2>结局 · 标签</h2>` 段落区（`.end-box`：`.end-line` 正文缩进 2em + `.end-meta` 右对齐小字，含「同结局人物」反链）。
- **结局总览页（endings.html）**：
  - 题区下先给一句出处声明（第110–120回逐人核对 + 与第119回名册吻合）。
  - **引文块（.end-quote）**：左 3px 朱砂边条 + 宋江谢恩表原文「十损其八」，作为全页的题眼；一处朱砂，不重复。
  - 分组复用 §5.12 的 `.act-head`（小字=类型释义 / h2=类型名 + `员数`），`id="catN"` 供锚点跳转；组内**保持座次序**，不按字数或回目排。
  - **条目卡（.end-item）**：同 `.event-card` 规范，栅格 `minmax(320px,1fr)`。卡内三段：`end-head`（座次金 + 姓名 21px 粗 + 绰号朱砂，同一基线）→ `end-d`（结局情节，ink-2 小字）→ `end-m`（地点 · 出处回，ink-3）。
  - 顺序固定为：阵亡 → 病故 → 坐化 → 被害 → 殉义 → 出家 → 归隐 → 善终（由重到轻，与 `endings.json` 的 cats 顺序一致）。

> **数据契约**：`data/endings.json` 是唯一数据源（不是 `heroes.json`——那个由 `parse_roster.py` 生成，手改会被覆盖）。新增类型须同时改 JSON 的 `cats` 与本文件。

---

### 5.16 地名（v1.4 新增）

水浒地名**虚实混杂**（东京是真的，梁山泊的地形是文学化的），是这个项目区别于史书类站点的核心。视觉上必须把「虚实」读出来，但**仍然不靠颜色**——地名已占用黛蓝这一色，三档虚实若再分色就撞破双色体系。

- **地名高亮（.ent.ent-place）**：黛蓝 `--dai` + 点状下划线（同 `.ent` 形态，仅换色）；hover 底 `rgba(58,90,110,.1)`，**不取朱砂**（§2.2）。悬浮卡沿用 `.ent-tip`，内容为「地名 · 类别 · 虚实 · 今 现代地名」，无现代对应者显示「无现实对应」。
- **虚实标签（.real-tag）**：同 `.end-tag` 规范（1px `--line` 方角、`--paper` 底、12px ink-2）。**三档（实/存疑/虚）共用一种样式**，靠文字区分，无 `end-none` 式的虚线变体（地名三档必填）。
- **地名图志（places.html）**：
  - 题区下声明条数 + 立目原则（逐个核验正文用词后立目）。
  - **题眼引文块**（复用 `.end-quote`）：一句点破「虚实混杂」的设计前提；一处朱砂，不重复。
  - 分组按**虚实三档**（复用 `.act-head`，`id="realN"` 供锚点），顺序 实 → 存疑 → 虚；组内先按类别聚拢（州府郡县 → 山泊关隘 → 川渡城关 → 寺观庙宇 → 庄寨市井 → 异域殊方），类内按出现回数降序（本题材的天然权重就是「出场多寡」）。
  - **条目卡（.place-item）**：同 `.event-card` 规范，栅格 `minmax(320px,1fr)`。卡内：`place-head`（地名 21px 粗宋 + 类别金色小字，同一基线）→ `place-modern`（今地行，**黛蓝**小字）→ `place-d`（一句话简介，ink-2 小字）→ `place-m`（出现 N 回，ink-3）。
- **地名词条页（wiki/〈地名〉.html）**：与人物词条页**同目录同套刻本框**（一致 §5.14「同一套视觉」思路）。`infobox` 字段：类别 / 虚实（+「同判定的地名」反链到图志锚点）/ 今地 / 别名 / 首见 / 出现（回数 + 正文提及次数）。正文区：简介 → **虚实判定依据**（`.end-box` + `.end-line`，即「为什么判它存疑」的考据行，本页的题眼）→ 相关名场面（`data/events.json` 中 `loc` 命中本地的名场面反链）→ 出现回目 chips。
- **事件页联动**：事件 `loc` 字段（如「孟州十字坡」）用 `match_place()` 认领其中的地名，输出为地名词条链接（§5.13 联动的一部分）。

> **数据契约**：`data/places.json` 是唯一数据源，字段 `name/cat/reality/modern/aliases/blurb/note`；`cats`（6 类）与 `realities`（3 档）定义在同文件内，新增须同时改 JSON 与本文件。**「虚实判定依据」（`note`）为立目必需**——没有依据的地名不收。
>
> **别名风险**：地名别名与人物别名的**长度包含关系**必须让更长者优先命中（如「梁山泊」须先于「梁山」），`render_site.py` 的 `build_matcher()` 已按长度降序排列；新增别名时要手验一遍是否会与人物名撞名（撞名会让链接指向错误的词条页）。

---

### 5.17 事件因果链（v1.5 新增）

因果链的视觉任务不是画一张好看的图，而是**让「因果」与「时序」肉眼可辨**——这是这个数据集唯一的质量红线（时间顺序 ≠ 因果）。

- **两类关系只靠线型区分，不赋颜色**（守 §2.2 朱砂唯一原则）：
  - `因果`（`.k-cause`）：**实线**——前者直接造成后者（含「为后者创造必要条件」）。
  - `递进`（`.k-step`）：**虚线**——同一线索的时间推进，无严格因果，但阅读顺序上必然先此后彼。
  - 链上相邻却无边（`.k-none`）：一条极淡虚线 +「（同一线索的下一步，无直接因果）」小字。**这个"空边"是刻意保留的**——它把"这里只是接着发生"明确说出来，比分不清更诚实。
- **关系标签（.kind）**：同 `.end-tag` 规范（1px `--line` 方角、`--paper` 底、12px ink-2），**不赋色**，文案即类型名。
- **线索链条（.chain / .chain-node / .chain-edge）**：纵向单列，整体靠左、右侧留白（宋版书的批注位）。节点为 `.event-card` 同规范的小卡（回数金小字 → 事件名 21px 粗宋 → 概述 ink-2 小字），节点之间是连线行：`▾` + 类型标签 + 关系动词。链条主干由 `.chain` 的 2px `--line` 左边线充当。
- **事件页因果区（.causal / .causal-col / .causal-item）**：两列（前因 | 后果），窄屏单列。每项一张小卡：`causal-head`（类型标签 + 对方事件名）→ `causal-verb`（关系动词，ink-2）→ `causal-note`（判定依据，ink-3 小字，可缺）。**无前因或无后果时该列不渲染；两边皆空则整个区块不出现**（全书首尾的 `误走妖魔` 只有后果、`魂聚蓼儿洼` 只有前因）。
- **跨线索因果（.cross-item / .cross-arrow）**：单行「因 ——动词——▶ 果」，两端事件名可点（黛蓝），中间的箭头承载关系动词。这是总览页的重点区块——单个线索视角看不到的连接（如"一套生辰纲如何把宋江牵进来"）。
- **判断标准区（.kind-legend）**：先把两类关系的线型样张画出来（实线/虚线 + 一句话定义），再往下看数据。**先立标准，后给结论**。
- **事件页信息表**加「线索」行：列出所属每条线索 + 「第 n/m 环」；事件跨线索（交汇点）时全部列出——交汇点往往正是最关键的因果节点（如 `大闹野猪林` 同时属林冲线与鲁智深线）。

> **数据契约**：`data/causal.json` 是唯一数据源。两套结构不可混用：`chains`=线索（有序事件名列表，只表示"这条线这么走"）、`links`=因果边（`from/to/kind/verb/note`，必须能说出"因为 A 所以 B"）。**判断标准：只能说"接着发生了 B"就只进 chains；能说"因为 A 所以 B"才立 links。** 二者混用会让边界失控，把时序误当因果。
>
> **构建期体检四项**：线索与边的事件名必须存在 → 无自环 → 无重复 from→to → **无孤岛**（既无前因也无后果的事件，说明线索没接上）；另查"未被任何线索收录的事件"。前几次数据出问题时，都是这五项之一先报出来的。

---

## 6. 装饰元素使用规则

| 元素 | 场合 | 限制 |
|---|---|---|
| 朱砂方印 | topbar、hero | 每屏 ≤1 方 |
| 菱形「◆」 | 回目题区收束、章节分隔 | 朱砂色，小字号 |
| 水波纹 SVG | 仅 hero 底部 | 透明度 ≤30% |
| 双边框 | 仅 .infobox | 不用于卡片 |

---

## 7. 动效

- 一律 `transition: 150ms ease`，仅限 `opacity / transform / border-color / background-color`。
- hover 上浮 ≤2px。无滚动动画、无加载动画、无骨架屏。
- `@media (prefers-reduced-motion: reduce)` 全局关闭 transition。

---

## 8. 可访问性

- 正文/强调色对比度 ≥ 4.5:1（见 2.1 标注）。
- `:focus-visible` 统一 `outline: 2px solid var(--cinnabar); outline-offset: 2px`。
- 悬浮卡信息同时在词条页可得（不依赖 hover 才能获取信息）。

---

## 9. 工程落点

- **唯一样式源**：`scripts/assets/style.css`；`render_site.py` 构建时拷贝到 `site/assets/`。
- 禁止行内样式、禁止页面级 `<style>`；新组件先加进本文件第 5 节再写 CSS。
- 暗色模式：暂不做；已 token 化，未来加 `@media (prefers-color-scheme: dark)` 覆写变量即可。
