# 交接文档 (HANDOFF.md)

> 写给完全没有上下文的新会话。读完这个文件 + `README.md` + `design.md` 就能无缝接手。
> 更新日期：2026-09-20（项目启动同日；最新节点：Phase 2-6 人物关系上线）

---

## 1. 我们在做什么

**水浒维基 (shuihu-wiki)**：把《水浒传》做成可交互、可探索的知识图谱——语法高亮、词条跳转、全文搜索、（远期）关系推理。

- **底本**：维基文库《水滸傳 (120回本)》，明·施耐庵，公有领域
- **方法论参考**：[baojie/shiji-kb](https://github.com/baojie/shiji-kb)（史记知识库），但关键认知：**水浒是小说不是史书**——实体体系（绰号/星号/山头/兵器）重新设计，时间用「叙事时序（第几回）」而非公元纪年，地名虚实混杂需标注
- **形态**：Python 脚本渲染的纯静态站，本地目录 `~/WorkBuddy/shuihu-wiki`

## 2. 已定决策（不要再翻案）

| 决策 | 结论 |
|---|---|
| 文字 | 简体为主 + 繁体对照（OpenCC 转换，繁体留底可切换） |
| 技术栈 | **Python 渲染静态站**（明确否决 Vue——内容型站点不需要前端框架）；除搜索页外零 JS |
| 部署 | **已做成 GitHub → Cloudflare Pages 自动部署**：`site/` 产物随仓库提交，CF 侧构建命令留空、输出目录 `site`。完整步骤见 `DEPLOY.md` |
| 功能优先级 | 108将图鉴 → 名场面事件库 → **人物关系图（下一个）**；行踪地图降级远期 |
| 视觉 | `design.md`「宋版书 × 数字刻本」是唯一标准；样式唯一源 `scripts/assets/style.css` |
| 提交安全 | **pre-commit 钩子强制安全审计**：`scripts/security_scan.py --staged` 扫暂存区（密钥/邮箱/手机号/IP/本机路径/敏感文件名 + 提交邮箱必须 noreply），命中即拒绝提交。钩子本体在 `.githooks/`，新机器 clone 后须执行一次 `git config core.hooksPath .githooks`；误报改脚本里的 `ALLOW` 放行，禁删检测规则 |

## 3. 已完成（全部当天完成）

| 阶段 | 产出 |
|---|---|
| Phase 0 语料 | 维基文库 122 页（序+引首+120回）全抓取成功；繁简双版本语料，简体 893,698 字；`index/回目索引.md` |
| Phase 1 MVP | 108将结构化（`data/heroes.json`，第71回石碣名单正则解析）；静态阅读器（繁简双版、段落编号、实体高亮+悬浮卡、上下回导航）；108将图鉴；108 词条页（信息卡+出场回目索引）；全文搜索（2.6MB 索引） |
| 设计规范 | `design.md` v1.2 + `scripts/assets/style.css` / `search.js` |
| Phase 2-1 名场面 | `data/events.json` 55 事件、四幕结构；长廊页 + 55 详情页（原文摘录自动定位+锚点、人物双向链接） |
| Phase 2-2 其他人物 | `data/people_extra.json` 51 人、8 分类；高亮/悬浮卡/词条页/图鉴分区/事件 cast 全接入 |
| **Phase 2-3 结局** | **`data/endings.json` 108 条（8 类：阵亡60/病故11/坐化1/被害3/殉义2/出家5/归隐4/善终22）；结局总览页 `endings.html`；图鉴卡结局标签 + 结局分布统计行；词条页刻本框「结局」行 + 独立结局区；topbar/首页入口** |
| **Phase 2-4 地名** | **`data/places.json` 106 条（6 类别 × 3 档虚实：实58/存疑17/虚31）；地名图志 `places.html` + 106 个地名词条页；地名高亮走黛蓝第二级实体（`.ent-place`）；事件 `loc` 字段联动地名词条（53/55，余 2 条为有意不收的区域名「河北」「淮西」）；topbar/首页入口** |
| **Phase 2-5 因果链** | **`data/causal.json`：10 条线索（chains，覆盖 55/55 幕）+ 54 条因果边（links，因果32/递进22，其中跨线索 6 条）；总览页 `causal.html`（判断标准 → 线索纵向链条 → 跨线索因果）；事件页加「线索」行 + 「因果」区（前因/后果 + 判定依据 + 关系动词）；topbar/首页入口** |
| **Phase 2-6 人物关系** | **`data/relations.json`：21 个集合（山头 15 / 阵营 6）+ 150 条关系边（结义9/亲属24/师徒9/主仆24/诱迫7/仇敌36/恩义15/旧交26），159 位人物全覆盖；总览页 `relations.html`（判断标准 → 山头与阵营 → 按类型分组的关系边 → 跨阵营连接 14 条）；两类人物词条页加「所属」行 + 「人物关系」区；topbar/首页入口。**159 位人物**人人有个人关系边**（补录后无一人仅靠集合）；其中黄门山蒋敬/马麟/陶宗旺三条从属边是为覆盖完整而付的重复代价，已在 `note` 里写明（见 §8.28） |
| **工程化** | **`scripts/check_links.py` 常驻链接体检（文件+锚点双校验）：573 页 / 90,459 条站内链接 / 12,667 条锚点，全 0 失效**（含 404 页的站点根绝对路径解析） |
| **当前规模** | **573 个 HTML 页**（含 404）；159 位人物全部有出场记录；108/108 条结局；106 处地名（104 处有正文出现记录）；55 幕事件全部接入因果链；150 条人物关系边，159 位人物全覆盖 |

## 4. 怎么跑（常用命令）

```bash
PY=~/.workbuddy/binaries/python/envs/default/bin/python

# 重建全站（改 corpus/ 或 data/ 后必跑）
$PY scripts/render_site.py

# 本地预览
python3 -m http.server 8000 --directory site   # → http://localhost:8000

# 其他脚本（一般不用重跑）
$PY scripts/fetch_corpus.py      # 抓原文（支持断点续跑）
$PY scripts/parse_roster.py      # 重新解析108将名单
$PY scripts/convert_t2s.py       # 繁转简（改了 VARIANTS 后要重跑 + 重渲染）
$PY scripts/validate_corpus.py   # 语料校验 + 生成回目索引
```

## 5. 目录结构

```
shuihu-wiki/
├── README.md            # 目标与边界（含已定决策）
├── HANDOFF.md           # 本文件
├── design.md            # 视觉设计唯一标准（改设计先改这里）
├── corpus/zh_TW|zh_CN/  # 语料（生成物，勿手改）
├── data/                # 全部内容数据（手工维护的核心资产）
│   ├── heroes.json          # 108将（parse_roster.py 生成）
│   ├── aliases_curated.json # 108将手工别名
│   ├── events.json          # 55 名场面（四幕结构）
│   ├── people_extra.json    # 51 位非108将人物
│   ├── endings.json         # 108将结局（8 类，逐人核对第110–120回）
│   ├── places.json          # 106 处地名（6 类别 × 实/存疑/虚 三档）
│   ├── causal.json          # 因果链：10 条线索（chains）+ 54 条因果边（links）
│   └── relations.json       # 人物关系：21 个集合（factions）+ 115 条关系边（links）
├── index/回目索引.md
├── scripts/             # fetch / convert / validate / parse_roster / render_site / check_links / assets(css,js)
└── site/                # 渲染产物（每次构建整体删除重建，勿放手工文件）
```

## 6. 当前卡在哪 / 已知局限

**无阻塞**。已知局限（都是有意的取舍，不是 bug）：

1. **指代消解缺失**：正文中「那妇人」等称呼无法归属（例：潘金莲名字仅出现 5 次/1 回，但她实际贯穿 24–26 回）。规则匹配的极限，词条页已注明"含误差"。解法是将来的 LLM 标注层（shiji-kb 方法论的核心下一步）。
2. 出场统计含误差（正则重叠匹配的消耗），页面已注明。
3. 「大刀/浪子/行者」等通用绰号只在「」引号内匹配（防误伤的正设计，代价是少量裸写漏标）。
4. 兵器/诗词/阵营实体未做（阵营已以 `relations.json` 的 factions 形式落地）。

## 7. 下一步计划（按优先级）

1. ~~**人物关系图**~~ ✅ 已完成数据层与页面层（Phase 2-6，2026-09-20）。剩余的是**可视化**：在 `relations.html` 上加一层静态图形（SVG 力导向/矩阵），或先做「以某人为中心的一跳关系子图」。**注意：可视化之前先把数据补全到 159 人全覆盖（现 121/159）**，且补人时仍守「宁缺勿滥」。
2. 因果链深化：给线索补"分叉/合流"标注（目前交叉事件只能在信息表里看到）；事件页因果区补原文依据锚点。
3. 高亮开关（design.md 有位置预留）。
4. 可选打磨：结局页支持按「征方腊前后」筛选、词条页结局区补原文摘录（复用 `find_excerpt`）、地名词条页补关系（某地名发生过什么）。

## 8. 绝对不要再踩的坑 ⚠️

1. **JSON 注释键必须过滤**：`data/*.json` 里 `_说明` 注释键曾被当成人物，说明文字被逐字拆成别名 → 全站数千个单字误标。所有读取处必须 `if name.startswith("_"): continue`；**新增读取处也要加**。
2. **回目对仗用语 ≠ 正文用语**：「垂杨柳」正文作「绿杨树」、「山神庙」正文作「古庙」、「狮子楼」正文根本没出现。**定关键词/别名前先 grep 正文验证**。
3. **人物本名 ≠ 正文称呼**：张天师正文只称「嗣汉天师/虚靖/张真人」，潘巧云正文多称「巧云」。别名按正文实际用词补。
4. **OpenCC t2s 漏转异体字**（已发现 鴈→雁）：往 `convert_t2s.py` 的 `VARIANTS` 映射里加，**不要手改语料文件**；改完重跑 convert + render。
5. **维基文库页面会泄漏 "Public domain" 授权模板**（div.licensetpl）：fetch 脚本已剥离+按行过滤，重抓时别删这些规则。
6. **`corpus/` 和 `site/` 都是生成物**：改内容一律改上游（data/、scripts/），手改会在下次构建时被覆盖。`site/` 每次构建整体 rmtree。
7. **样式唯一源 `scripts/assets/style.css`**：禁止行内样式、禁止直接改 `site/assets/`（构建时被拷贝覆盖）。新组件先写进 design.md 第 5 节再写 CSS。
8. **验证数量用 `grep -o | wc -l`**：macOS `grep -c` 按行计数，卡片 join 到一行时会被骗（55 张卡显示成 4）。
9. **对微小异常敏感**：构建日志里 "有出场记录的人物 109/108" 这种 +1 异常帮我们抓到了全站级误标 bug。计数不对必须查，不要放过。
10. **抓取限速 1s/页 + UA 标识**：维基文库是公益服务，保持礼貌；脚本已内置，别删。
11. **结局数据放 `data/endings.json`，不要塞进 `heroes.json`**：后者由 `parse_roster.py` 生成，手加的字段会在下次重解析时被覆盖。同理 `endings.json` 的 `cats` 顺序即结局页的展示顺序，改顺序就改这个数组。
12. **结局归类以第119回宋江谢恩表为准尺**：该回完整列出「阵亡59/病故10/坐化1/出家1/归隐4/留京5/见在朝觐27」，合计正好108，是天然的对账表。新增或改动结局时，用机器对撞一遍（`render_site.py` 启动即校验缺项/多项/非法类型），别靠肉眼。注意口径差异：**阵亡含呼延灼（征方腊后破金阵亡）、病故含关胜（受封后醉坠得病身亡）**，这两人不在第119回名册内（当时尚在世），是 60/11 而非 59/10 的原因。
13. **验证站内链接要剥掉 `?query`**：`../search.html?q=名字` 这类查询串若不剥掉，会被断链检查误报 159 条（真实断链为 0）。
14. **地名别名必须验「长度包含关系」与「撞名」**：别名表里「梁山」会被「梁山泊」抢先命中（`build_matcher()` 按长度降序 ✓），但地名的**类名**（北京/南京/西京）绝不能塞进「东京」的别名表——已分离到各自 canonical。新增地名别名时必须跑一遍 `render_site.py` 的启动校验（非法类别/非法虚实/别名撞人名会直接报错）。
15. **地名不在正文出现的，是有意立目的记录项**：「狮子楼」「玄女庙」在回目里是关键词，但正文一次都没出现（坑 #2）。这类条目不要因为「出现 0 回」就删——它们正是「回目用语 ≠ 正文用语」的活证据，`blurb`/`note` 里要写明这一点（构建日志里 `104/106 处有出现记录` 的 2 条缺口即此二者，属正常）。
16. **地名挖掘别靠自动词频**：自动候选里混入大量人名（杨林、蒋门神）、通用词（古庙 10 次命中其实指 4 处不同地点）。做法是人工列候选 + 逐条 grep 正文验证别名，确定「收」与「不收」的边界；通用词一律不立目。
17. **在自由文本里认领地名必须走别名**：`match_place()` 最初只比对规范名，于是事件 loc 写「郓城」「大名府」（正文简称），而地名规范名是「郓城县」「北京大名府」——正向包含判定全部落空，7 条事件地点静默不链接。修法是遍历 `(name, *aliases)` 取命中最长者。凡「从自由文本反查实体」，都要问一遍：这里的文本用的是规范名还是别名？
18. **二字地名别名可能双义，宁缺勿滥**：「揭阳」同时指揭阳镇与揭阳岭，若两条都收「揭阳」作别名，`canonical_of` 字典会后写覆盖先写 → 一处链接随机指向两地。故两条目只收全称，注释里写明这个取舍（同坑 #14 的延伸）。
19. **合成规范名 vs 正文用词要分开对待**：「南京应天府」正文 0 见（正文作『南京』3 见 / 『应天府』4 见），但作词条名可读性更好——此时**别名必须覆盖正文实际用词**，并在 `note` 里记下正文出现数。构建后跑一遍别名正文命中体检：`别名零命中` 只允许是有意立目的差异记录项（狮子楼/玄女庙）。
20. **时序 ≠ 因果，两者在数据里必须分家**：`causal.json` 的 `chains`（线索，有序事件名）与 `links`（因果边，必须能说出「因为 A 所以 B」）混用会让边界失控——把"接着发生"写成因果是最容易犯的错。配套的正设计：链上相邻但无因果边时，渲染成虚线 +「（同一线索的下一步，无直接因果）」，**把不确定明确说出来比分不清更诚实**。立边前先自问一句：这是"A 导致 B"还是"A 之后是 B"？
21. **跨线索的边才是信息量最大的**：54 条边里 6 条两端不在同一线索（杨志卖刀→智取生辰纲、江州劫法场→沂岭杀四虎、元夜闹东京→燕青遇道君…），总览页把它们单列成区块。做任何关系类数据（人物关系图同理）都要留一格给"跨集合连接"——同集合内部的连接读者自己就能猜出来。
22. **因果数据必须做四项体检**：事件名存在性、无自环、无重复 from→to、**无孤岛**（既无前因也无后果的事件＝线索没接上）。再加一项"未被任何线索收录的事件"。本次 55/55 全覆盖、54 条边零孤岛，是靠这五项反复跑出来的，不是肉眼看的。
23. **文档编辑后必须回读验证**：本项目用工具批量改文档时遇到过「返回成功但未落盘」（README/HANDOFF/design.md/style.css 都出现过），导致过样式类名引用了但 CSS 里根本没有（页面上静默失效、构建仍然成功）。**改完立刻 grep 回读关键行**，别信"操作成功"。
24. **关系类数据同样要「集合与边分家」**：`relations.json` 的 `factions`（山头/阵营，名单即集合）与 `links`（两个人的关系边）不可互相替代。判据一句话：**名单能说清的，绝不拆成边**——二龙山 7 人拆开是 21 条边、108 人拆开是近 6000 条，全是读者自己就能猜出来的连接，是噪音不是信息。反过来，只要能说出「A 对 B 做了什么」就该立边，别偷懒塞进名单。
25. **凡带出处回目的数据，都要加「回目无交集」体检**：检查边两端的人是否真在标注回目里出现过。本次就是这一项抓出「武松→潘金莲 第26回」——杀嫂确在第26回，但该回正文只称「那妇人」，「潘金莲」三字全书仅见于第24回（坑 #1 指代消解缺失的典型）。处理办法是把回目放宽成 `024-026` 并在 `note` 里写明这个口径差异，**不要靠肉眼判断出处**。
26. **f-string 里不要嵌套同种引号**：写渲染代码时误写成 `f'...{l["note"]}...'` 的层层嵌套，虽然 Python 3.12+ 语法允许，但极易写坏（本次就产出了一行不可读的 `chr()` 拼接垃圾）。**嵌套一律先提出变量**（`note = ... if ... else ""`），再拼进 f-string。
27. **「默认归属」这类隐式规则必须写在代码里并加注释**：108 将若未另归任何山头，默认属「梁山泊」（那条 `members` 为空的集合）——规则在 `factions_of()` 里。没有它，跨阵营判定会把宋江当成"无阵营"，朝廷↔梁山的一整片关系边就全漏了。同理 `is_cross_faction()` 要求**两端各有一个 `cls="阵营"` 的归属**才成立，否则「鲁智深（二龙山）↔林冲（梁山泊）」这种同一伙人的内部连接会被误判成跨阵营。
28. **补覆盖率时，先分清"覆盖"的两个口径**：① 有**个人关系边**；② 只在**集合**里（有山头/阵营归属、无个人边）。二者都能让词条页有内容，但只有前者算"落到两个人头上"。补录（121/159 → 159/159）时的做法：**先按正文提及次数排序，只给"有戏份且说得清"的人立边**；实在说不清的一律交名单，**不为凑满数字编关系**。唯一破例是黄门山蒋敬/马麟/陶宗旺三条从属边（同一句话重复三遍），这是为覆盖完整付的代价，**必须在 `note` 里写明**（读者看到才知道这是取舍，不是数据错误）。

## 9. 新会话上手顺序

1. 读 `README.md`（目标/边界/决策）→ 本文件 → `design.md`
2. 跑 `render_site.py` + `check_links.py` + `http.server`，点一遍首页/图鉴/结局/地名图志/因果链/**人物关系**/名场面/任意章节/任意事件页/任意人物词条页，建立体感
3. 更细的过程日志（每次踩坑与修复）在工作记忆：`.workbuddy/memory/2026-09-20.md`
4. 然后从第 7 节第 1 项「人物关系图的可视化 + 补人」继续干

## 10. 部署与同步（2026-09-20）

| 项 | 内容 |
|---|---|
| **线上地址** | **https://shuihu-wiki.pages.dev**（2026-09-20 23:00 首次上线，全站验证通过） |
| 仓库 | `https://github.com/empttttty/shuihu-wiki`（public）；remote `git@github.com:empttttty/shuihu-wiki.git`，SSH 免密（本机 `~/.ssh/id_ed25519`） |
| 托管 | Cloudflare Pages，Git 集成，监听 `main` 分支 |
| 关键配置 | Framework preset `None`；**Build command 留空**；Build output directory `site` |
| 完整步骤/排查 | `DEPLOY.md` |

**部署相关的硬规则（与坑清单同级）：**

1. **`site/` 是要提交的构建产物**——这是本项目唯一一处「生成物进仓库」的例外，为的是让 Cloudflare 不跑 Python（OpenCC 依赖在 CF 构建镜像里不可靠）。`site/` 仍然是生成物，**手改依旧会被下次重建覆盖**（坑 #6 不变）。
2. **上线四步不可跳**：`render_site.py` → `check_links.py`（必须全 0）→ `git commit` → `git push`。CF 侧不做任何校验，push 出去就是线上，体检是唯一守门人。
3. **中文路径已验证通过（2026-09-20）**：词条页文件名是中文（`site/wiki/宋江.html`），站内链接写作未编码的 `href="wiki/宋江.html"`；CF Pages 走 URL 解码匹配，实测 `/wiki/宋江` 返回 200 且 title 与正文均正确——**不需要拼音 slug 降级方案**。（附带行为：CF 会把 `.html` 后缀 308 归一化到无后缀路径，站内链接多一跳，功能与相对路径解析均正常。）
4. **`.workbuddy/`（AI 工作记忆）不入仓库**，已在 `.gitignore` 排除。
5. **不要复用 md2mp 那个 Pages 项目**：它是 Direct Upload 类型，接不了 Git。
6. **push 走 SSH，不走 gh**：本机 `~/.ssh/id_ed25519` 已加到 GitHub 账号，`git push` 免密；`gh` CLI 虽已装（v2.101.0）但**未登录，且不影响链路**。
7. **待办：缺 `site/404.html` 导致软 404**：实测 CF 对未匹配路径返回 **200 + 首页内容**（`/wiki/nonexistent-xyz.html`、`/no-such-dir/no-such-page.html` 均如此）。补一个 404 页可让错误路径返回真正的 404 状态码；生成逻辑要写进 `render_site.py`（守「内容改上游」，别手写进 `site/`）。
